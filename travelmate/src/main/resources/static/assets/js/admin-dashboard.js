/**
 * ⚠️ LEGACY FILE — KHÔNG CÒN DÙNG
 *
 * File này trước đây cập nhật dashboard từ room-store + booking-store (localStorage).
 * Hiện tại /admin/dashboard lấy thống kê thật từ MySQL qua Thymeleaf.
 *
 * KHÔNG import file này vào admin/dashboard.html.
 * Giữ lại file để tham khảo nếu cần.
 *
 * Chuyển sang MySQL ở: AdminPageController.java + BookingService (dashboard methods)
 */

// === TOÀN BỘ CODE ĐÃ TẮT (LEGACY) ===
