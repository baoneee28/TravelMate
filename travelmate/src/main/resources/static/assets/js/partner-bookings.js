/**
 * ⚠️ LEGACY FILE — KHÔNG CÒN DÙNG
 *
 * File này trước đây render booking cho partner từ booking-store.js (localStorage).
 * Hiện tại /partner/bookings lấy dữ liệu thật từ MySQL qua Thymeleaf.
 *
 * KHÔNG import file này vào partner/bookings.html.
 * Giữ lại file để tham khảo nếu cần.
 *
 * Chuyển sang MySQL ở: PartnerPageController.java + BookingService.getBookingsForPartner()
 */

// === TOÀN BỘ CODE ĐÃ TẮT (LEGACY) ===
