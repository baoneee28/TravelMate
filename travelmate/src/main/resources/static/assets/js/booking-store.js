/**
 * ⚠️ LEGACY FILE — KHÔNG CÒN DÙNG LÀM NGUỒN DỮ LIỆU CHÍNH
 *
 * File này trước đây lưu booking vào localStorage để demo.
 * Hiện tại:
 *   - /admin/bookings: lấy dữ liệu thật từ MySQL (Thymeleaf)
 *   - /partner/bookings: lấy dữ liệu thật từ MySQL (Thymeleaf)
 *   - /admin/dashboard: lấy thống kê thật từ MySQL (Thymeleaf)
 *
 * KHÔNG import file này vào bất kỳ trang nào đang dùng dữ liệu MySQL.
 * Giữ lại file để tham khảo structure nếu cần.
 *
 * Chuyển sang MySQL ở: BookingService.java + BookingRepository.java
 */

// === TOÀN BỘ CODE DƯỚI ĐÂY ĐÃ ĐƯỢC TẮT (LEGACY) ===
// Nếu cần khôi phục demo localStorage, bỏ comment block bên dưới.

/*
(function () {
  const STORAGE_KEY = 'travelmateBookings';
  // ... (xem git history để khôi phục nếu cần)
})();
*/
