/**
 * Package security - chua cac class lien quan den bao mat.
 *
 * Sau nay se chua:
 * - JwtTokenProvider: tao va xac thuc JWT token
 * - JwtAuthenticationFilter: filter kiem tra token moi request
 * - CustomUserDetailsService: load thong tin user cho Spring Security
 *
 * Hien tai SecurityConfig nam trong package config vi chua phuc tap.
 * Khi tich hop JWT se chuyen phan security sang day.
 */
package com.travelmate.security;
